# Environment Variables for nix-devbox
#
# Copy this file to .env and fill in your actual SSH public keys.
# The .env file is gitignored and should never be committed.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your actual keys
#   source .env  # Or use direnv
#   nix flake check
#   sudo nixos-rebuild switch --flake .#devbox
#
# If keys are not set, placeholder keys will be used (build succeeds,
# but SSH login will fail at runtime with a clear error).

# coal's SSH public key (ed25519 recommended)
# Get your key: cat ~/.ssh/id_ed25519.pub
SSH_KEY_COAL=""

# violino's SSH public key (ed25519 recommended)
SSH_KEY_VIOLINO=""

# Optional: Set to "true" to fail the build if SSH keys are missing
# Useful for production deployments where you want to ensure keys are set
# NIX_STRICT_KEYS="true"
