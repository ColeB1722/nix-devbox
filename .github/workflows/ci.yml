# CI workflow for nix-devbox
#
# Runs on:
# - Push to main or release/* branches
# - PRs targeting main or release/* branches
#
# Pipeline: format check → flake check → build (single job for efficiency)
#
# Feature branches get no CI until they open a PR.
# Pre-commit hooks handle local validation.
#
# Uses DeterminateSystems determinate-nix-action for modern flake support
# with integrated caching.

name: CI

on:
  push:
    branches:
      - main
      - 'release/**'
  pull_request:
    branches:
      - main
      - 'release/**'

# Required for FlakeHub Cache authentication
permissions:
  id-token: write
  contents: read

jobs:
  ci:
    name: Format, Check, Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Nix with caching
        uses: DeterminateSystems/determinate-nix-action@v3

      - name: Check formatting
        run: nix fmt -- --check .

      - name: Run flake check
        run: nix flake check

      - name: Build NixOS configuration
        run: nix build .#nixosConfigurations.devbox.config.system.build.toplevel

  # Future: deploy job
  # deploy:
  #   name: Deploy to Devbox
  #   runs-on: ubuntu-latest
  #   needs: ci
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Deploy via SSH/Tailscale
  #       run: echo "TODO: implement deployment"
